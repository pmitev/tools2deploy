#!/bin/bash

source common.conf
PREFIX=${PREFIX_ROOT}/sdkman

# Check if directory already exists
if [[ -d ${PREFIX} ]]; then
  echo "Uninstalling sdk and all tools"
  rm -rf ${PREFIX}
  rm ${HOME/env/sdkman}
else
  echo "${PREFIX} does not exists"
fi
